{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "incremental": false,
    "composite": false,
    "noEmit": false,
    // Map @orbyte-ai/shared subpaths to their built .d.ts so the tsup/rollup dts
    // bundler resolves and INLINES these types into Opal's published .d.ts. Its
    // resolver doesn't follow the package `exports` map, so without this it leaves
    // `from "@orbyte-ai/shared/contracts"` imports in the output — which break
    // consumers, since shared is only a devDependency (not shipped with Opal).
    "paths": {
      "@opal/*": ["./src/*"],
      "@orbyte-ai/shared/contracts": ["../shared/dist/contracts/index.d.ts"]
    }
  }
}
